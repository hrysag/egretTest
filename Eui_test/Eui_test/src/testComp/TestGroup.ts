class TestGroup extends eui.Group{

    /**
     * 在ui editor裡面要可見有兩條路
     * 1.實踐UIComponent interface
     * 2.實踐 IViewport interface
     * 只有這兩條路才會被接受
     * 
     * 
     */
    constructor(){
        super();
    }

}